package org.example

enum class TokenType {
    ID,
    EQUALS,
    LESS_THAN,
    GREATER_THAN,
    LESS_THAN_EQUALS,
    GREATER_THAN_EQUALS,
    NUMBER,
    LPAREN,
    RPAREN,
    SIGMA,
    PI,
    COMMA,
    INVALID
}